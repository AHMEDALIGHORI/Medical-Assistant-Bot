# Medical Assistant Bot (NLP THEORY CPC) — Python dependencies
# Use the same venv as nlp-service or: python -m venv .venv && pip install -r requirements.txt

# Streamlit needs starlette>=0.41 (DEFAULT_EXCLUDED_CONTENT_TYPES in gzip middleware)
streamlit>=1.40.0
starlette>=0.41.0

# RAG + ML
chromadb==0.6.3
sentence-transformers==3.3.1
transformers==4.50.3
torch>=2.2.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.23.0

# Ingest + config
pyyaml>=6.0
pypdf>=5.0.0
httpx>=0.27.0

# Chroma telemetry (keep <6 unless tested)
posthog>=3.0.0,<6.0.0
